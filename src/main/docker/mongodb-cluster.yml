version: '2'
services:
    keken2-mongodb:
        image: mongo:3.2.10
        ports:
            - "27017:27017"
        command: mongos --configdb keken2-mongodb-config
    keken2-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    keken2-mongodb-config:
        image: mongo:3.2.10
        container_name: keken2-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
